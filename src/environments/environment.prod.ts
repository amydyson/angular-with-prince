export const environment = {
  production: true,
  // TODO: Update this URL after deploying to Heroku
  // Replace 'your-heroku-app-name' with your actual Heroku app name
  apiUrl: 'https://your-heroku-app-name.herokuapp.com/api'
  
  // For reference, the old relative URL was:
  // apiUrl: '/api' // This only works when backend and frontend are on same domain
};